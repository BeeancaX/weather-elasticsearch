PUT /weather_data/_doc/1
{
  "mappings": {
    "properties": {
      "location": { "type": "geo_point" },
      "timestamp": { "type": "date" },
      "temperature": { "type": "float" },
      "humidity": { "type": "float" },
      "pollution_index": { "type": "integer" }
    
    }
  }
}

 "weather_data" index with specified mappings:

"location": A field of type "geo_point" for storing geographical coordinates.
"timestamp": A field of type "date" for storing date and time information.
"temperature" and "humidity": Fields of type "float" for storing temperature and humidity as floating-point numbers.
"pollution_index": A field of type "integer" for storing pollution index as whole numbers.
